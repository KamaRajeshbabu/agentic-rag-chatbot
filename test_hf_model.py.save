import requests
import json

API_TOKEN = "your_hf_api_token_here"  # ← replace this with your actual token

headers = {
    "Authorization": f"Bearer {API_TOKEN}",
    "Content-Type": "application/json"
}

model = "mistralt
